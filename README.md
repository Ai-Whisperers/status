# AIW Status (public)

Sanitized, auto-generated status pages for the AIW agent org.

| File | Contents | NEVER contains |
|---|---|---|
| `run-ledger.html` | job counts, success rate, LLM cost band | job names, clients, error traces |
| `mission-control.html` | same aggregates, bench score, safety-stack status | job names, clients, error traces |

## Sanitization rules (enforced by generator)

1. No cron job names 2. No error text 3. No client/org identifiers 4. No prompts or outputs 5. Aggregate numbers only

Regenerated from the private fleet by `scripts/observability/` cron jobs. Contact: via ai-whisperers.org.
